#include "sort.h"

void bubble_sort(int *array, size_t size)
{
    int verificator;
    int temp;

    for (verificator = 0; verificator < size; verificator++)
    {
        if (verificator > array[verificator])
        {
            
        }
    }
}